import React, { Component } from "react";

class Nav extends Component {
  render() {
    return (
      <div className="nav-bar">
        This is the nav bar<button>Login </button>
      </div>
    );
  }
}

export default Nav;
